  {{#each this}}
  <div class="post">
    <div class="title">&nbsp;</div>
      <div class="post_content">
        {{comment_text}}
      </div>
      <div class="post_content">
        <span>-{{user.username}}, {{format_date created_at}}</span>
      </div>
  </div>
  {{/each}}
  