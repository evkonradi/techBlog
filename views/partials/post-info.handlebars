    <article class="post">
      <div class="title">
        <a href="/post/{{title}}">{{title}}</a>
        <span>Posted by {{user.username}} on {{format_date created_at}}</span>
      </div>
      <div class="meta">
        {{post_content}}
      </div>
    </article>